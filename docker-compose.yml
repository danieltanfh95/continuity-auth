services:
  datalevin:
    image: huahaiy/datalevin:0.9.22
    platform: linux/amd64
    container_name: cauth-datalevin
    command: ["dtlv", "serv", "-r", "/data", "-p", "8898"]
    ports:
      - "8898:8898"
    volumes:
      - datalevin-data:/data
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "8898"]
      interval: 5s
      timeout: 2s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cauth-app
    depends_on:
      datalevin:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      CONTINUITY_AUTH_HTTP_HOST: 0.0.0.0
      CONTINUITY_AUTH_HTTP_PORT: 8080
      # Dev-default credential. Override CONTINUITY_AUTH_DTLV_PASSWORD in .env (or your
      # secrets manager) before deploying anywhere a stranger can reach.
      CONTINUITY_AUTH_DTLV_URI: dtlv://admin:${CONTINUITY_AUTH_DTLV_PASSWORD:-change-me}@datalevin:8898/continuity-auth
      CONTINUITY_AUTH_LOG_LEVEL: info
      CONTINUITY_AUTH_OTEL_ENDPOINT: ""
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:8080/healthz"]
      interval: 10s
      timeout: 2s
      retries: 5

volumes:
  datalevin-data:
